const set = {
  minTime: Infinity,
  maxTime: -Infinity,
  map: {},
  times: [],
  data: [{ type: "line" }],
};

// Price
series = [value];

// Candlestick
series = [open, high, low, close];

// Footprint
// rows = [4]; // rows per time
series = [3000, 3001, 3002, 30003]; // price, price
// seriesBounds = [3000, 3003]; // min, max
volume = [1.2, -1.3, 27, -38, 29, -42, 3.2, -6]; // buy, sell, buy, sell
// volumeBounds = [-42, 29]; // min, max
